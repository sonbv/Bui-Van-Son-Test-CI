console.log('A1. Tìm số đối diện');

function findOppositeNumber(n, inputNumber) {
    if (inputNumber < (n / 2)) {
        return n / 2 + inputNumber;
    } else {
        return inputNumber - n / 2; 
    }
}

console.log('A2. Viết hàm nhận chuỗi vào hai chuỗi S1 và S2, trả về là một chuỗi mới chứa các kí tự xen kẽ cùng vị trí của 2 chuổi với nhau');

let x = abc
let y = "123"
